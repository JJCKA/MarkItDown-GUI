# MarkItDown UI 风格总结

> 项目：MarkItDown GUI v2.1.0
> 技术栈：React 18 + TypeScript + Vite + Electron + Zustand
> 设计灵感：Typora 极简风格

---

## 1. 设计理念

整体追求 **Typora 式的极简、沉浸、无干扰** 体验。界面力求"隐形"——让内容成为主角，控件退居幕后。所有元素都克制、精致，避免视觉噪音。

---

## 2. 整体布局

### 2.1 页面结构（从上到下）

| 区域 | 高度 | 说明 |
|------|------|------|
| TitleBar | 32px | 自定义标题栏，含图标+窗口名+最小化/最大化/关闭 |
| MenuBar | 24px | 极细菜单栏（文件/编辑/视图/转换/帮助） |
| 主体区域 | flex: 1 | 左侧 Sidebar + 可拖拽分割线 + 右侧内容区 |
| LogPanel | 0~140px | 可折叠日志面板 |
| BottomBar | 40px | 状态栏 + 操作按钮 |

### 2.2 侧边栏（Sidebar）

- 宽度范围：250px ~ 600px，默认 288px
- 支持拖拽调整宽度
- 可完全折叠（宽度过渡动画 0.25s ease）
- 内含 Tab 切换：「文件」/「已转换」
- 底部操作栏：打开文件/文件夹、设置、历史记录、统计

### 2.3 内容区

- 默认为 Markdown 预览模式
- 支持源码编辑模式、原始 vs 结果对比模式
- 对比模式下有可拖拽分割线

---

## 3. 颜色体系

### 3.1 核心色板

| 变量 | 色值 | 用途 |
|------|------|------|
| `--bg` | `#ffffff` | 主背景 |
| `--sidebar` | `#f7f8f9` | 侧边栏背景 |
| `--text` | `#2b2b2b` | 主文字 |
| `--muted` | `#6b6b6b` | 次要文字 |
| `--faint` | `#999999` | 辅助/占位文字 |
| `--border` | `#e5e5e5` | 边框/分割线 |
| `--hover` | `#ebebeb` | 悬停背景 |

### 3.2 强调色

| 变量 | 色值 | 用途 |
|------|------|------|
| `--accent` | `#4183C4` | 主强调色（蓝色），用于按钮、链接、选中指示条、进度条 |
| `--accent-pink` | `#e25571` | 次强调色（粉色），用于 LLM 相关操作 |

### 3.3 状态色

| 变量 | 色值 | 用途 |
|------|------|------|
| `--success` | `#4CAF50` | 成功（绿色对勾） |
| `--danger` | `#c42b1c` | 错误/危险操作（关闭按钮悬停、删除确认） |
| `--selected` | `#e8e8e8` | 选中项背景 |
| `--card` | `#f5f5f5` | 卡片/设置区块背景 |
| `--code-bg` | `#f6f8fa` | 代码块/引用块背景 |

### 3.4 色调特征

- **整体偏白偏灰**，营造干净、通透的视觉感受
- 没有大面积的饱和色，强调色仅在关键交互点出现
- 层次感通过微妙的灰度差异实现（`#f7f8f9` → `#f5f5f5` → `#ebebeb` → `#e5e5e5`）

---

## 4. 字体排版

### 4.1 字体族

| 变量 | 字体栈 | 用途 |
|------|--------|------|
| `--font-ui` | `'Microsoft YaHei UI', 'Segoe UI', 'PingFang SC', sans-serif` | 界面文字 |
| `--font-mono` | `'Consolas', 'Cascadia Code', 'Fira Code', monospace` | 代码、数据、日志 |
| Markdown 正文 | `'Georgia', 'Noto Serif SC', 'Source Han Serif SC', var(--font-ui)` | Markdown 预览正文（衬线体） |

### 4.2 字号体系

| 变量 | 大小 | 用途 |
|------|------|------|
| `--font-size-xs` | 11px | 标签、提示、快捷键标注 |
| `--font-size-sm` | 12px | 菜单文字、状态栏文字 |
| `--font-size-base` | 13px | 正文/表单默认 |
| `--font-size-md` | 14px | 源码编辑器 |
| `--font-size-lg` | 16px | 文件树名称 |
| `--font-size-xl` | 18px | Tab 按钮、操作按钮文字 |
| `--font-size-xxl` | 20px | 页面标题（设置/历史等） |

### 4.3 排版风格

- 行高宽松：Markdown 预览 `line-height: 1.75`
- 标题间距充裕，h1/h2 带底部分割线
- 正文 15px，比 UI 文字大一档，阅读舒适

---

## 5. 间距与尺寸

### 5.1 固定尺寸

| 元素 | 尺寸 |
|------|------|
| 标题栏高度 | 32px |
| 菜单栏高度 | 24px |
| 底部栏高度 | 40px |
| 侧边栏默认宽度 | 288px |
| 侧边栏宽度范围 | 250px ~ 600px |

### 5.2 内部间距模式

- **按钮内边距**：`0 8px` ~ `0 18px`，高度 28px ~ 36px
- **列表项**：高度 28px，水平内边距 4px ~ 16px
- **卡片内边距**：`14px 16px`
- **菜单项**：高度 32px，水平内边距 12px
- **整体留白**：克制但充足，不拥挤也不空旷

---

## 6. 圆角

| 变量 | 大小 | 用途 |
|------|------|------|
| `--radius-sm` | 4px | 按钮、输入框、小标签 |
| `--radius-md` | 6px | 操作按钮（转换等）、进度条 |
| `--radius-lg` | 8px | 卡片、弹出菜单、弹窗、主窗口 |

- 统一使用圆角，无尖锐边角
- 弹出菜单和卡片使用较大的圆角（8px），显得柔和

---

## 7. 阴影

| 变量 | 值 | 用途 |
|------|-----|------|
| `--shadow-window` | `0 0 20px rgba(0,0,0,0.15)` | 主窗口 |
| `--shadow-popup` | `0 4px 16px rgba(0,0,0,0.12)` | 弹出菜单、弹窗 |
| `--shadow-card` | `0 1px 3px rgba(0,0,0,0.06)` | 卡片 |

- 阴影非常克制，不使用深阴影
- 弹出层阴影较明显，用于层级区分

---

## 8. 滚动条

```css
宽度/高度：6px
轨道：透明
滑块：#c0c0c0，圆角 3px
滑块悬停：#a0a0a0
```

- 极细滚动条，不干扰内容
- 仅在悬停时可见

---

## 9. 控件风格

### 9.1 按钮

- **主操作按钮**（转换）：`--accent` 蓝色背景 + 白色文字 + `--radius-md` 圆角 + 18px 粗体
- **次操作按钮**（LLM 转换）：`--accent-pink` 粉色背景 + 白色文字
- **图标按钮**：透明背景 + `--faint` 灰色图标 + 悬停变深 + `--radius-sm` 圆角
- **危险按钮**（清除缓存/历史）：透明背景 + 红色文字 + 红色边框
- **菜单按钮**：透明背景 + 悬停 `--hover` 灰色

### 9.2 输入框

```css
宽度：200px，高度 28px
边框：无
圆角：4px
背景：白色
内边距：0 8px
字号：13px
```

- 无边框设计，融入卡片背景
- 聚焦时无明显轮廓变化

### 9.3 下拉选择框

- 与输入框样式完全一致
- 使用系统默认下拉箭头

### 9.4 复选框

- 使用浏览器原生样式
- `accent-color: var(--accent)` 蓝色高亮

### 9.5 标签/徽章

- 缓存标签：蓝色背景 `rgba(65, 131, 196, 0.1)` + 蓝色文字
- LLM 标签：粉色背景 `#fce4ec` + 粉色文字
- 成功/失败：绿色/红色文字对勾

---

## 10. 图标风格

### 10.1 设计规范

- **尺寸**：统一 16px × 16px（文件树）或 18px × 18px（工具栏）
- **风格**：线性图标（stroke），不使用填充
- **描边**：`strokeWidth="1.5"` ~ `"1.8"`
- **颜色**：`currentColor`，继承父元素的 `--muted` 灰色
- **端点**：`strokeLinecap="round"`，圆润柔和
- **来源**：内联 SVG，无外部图标库依赖

### 10.2 图标类型

- 文件夹、文件（PDF/Word/PPT/Excel/图片/音频/视频/HTML/JSON/文本）
- 操作图标：打开文件夹、齿轮设置、时钟历史、柱状统计
- 编辑器工具栏：复制、下载/导出
- 视图切换：眼睛（预览）、代码（源码）、分屏（对比）
- 导航：左右箭头
- 窗口控制：最小化、最大化/还原、关闭（线条绘制）

---

## 11. 动画与过渡

### 11.1 过渡时间

| 场景 | 时间 | 缓动 |
|------|------|------|
| 侧边栏折叠/展开 | 0.25s | ease |
| 分割线颜色变化 | 0.15s | — |
| 分割线宽度变化 | 0.10s | — |
| 按钮/菜单项悬停 | 0.10s ~ 0.15s | — |
| 进度条宽度 | 0.30s | ease |
| Tab 指示条滑动 | 0.20s | ease |
| Log 面板展开/收起 | 0.20s | ease |
| Toast 提示出现/消失 | 0.20s | — |

### 11.2 动画特征

- **极简过渡**：几乎全部是简单的 `transition` 属性变化
- **无复杂动画**：没有 keyframes、没有弹跳/弹性效果
- **响应迅速**：过渡时间普遍在 0.1s ~ 0.25s，感觉干脆利落
- **仅用于状态变化**：悬停、展开/折叠、进度更新

---

## 12. 交互模式

### 12.1 悬停反馈

- 所有可交互元素都有悬停态：背景从透明变为 `--hover`（`#ebebeb`）
- 图标按钮悬停时颜色从 `--faint` 变为 `--muted`
- 关闭按钮悬停时背景变为 `--danger` 红色，图标变白

### 12.2 选中状态

- 文件树选中项：`--selected`（`#e8e8e8`）灰色背景
- 侧边栏 Tab：选中项文字变粗（700）+ 颜色变深（`--text`），底部有滑动指示条

### 12.3 拖拽交互

- 侧边栏宽度可拖拽调整，悬停时分割线变蓝变宽
- 文件/文件夹拖放上传，显示蓝色虚线遮罩 + 图标提示
- 对比模式分割线可拖拽

### 12.4 弹出层

- 弹出菜单：圆角 8px + 阴影 + 1px 边框
- 点击外部自动关闭
- 确认弹窗：半透明黑色遮罩 + 居中白色卡片

---

## 13. Markdown 预览样式

### 13.1 标题

- h1：1.85em，底部 1px 分割线
- h2：1.5em，底部 1px 分割线
- h3：1.25em
- 全部粗体（700），间距充裕

### 13.2 代码

- 行内代码：浅灰背景 `#f6f8fa` + 红色文字 `#c7254e` + 3px 圆角
- 代码块：深色背景 `#2b2b2b` + 浅色文字 `#a9b7c6` + 右上角语言标签

### 13.3 引用块

- 左侧 4px 蓝色竖线（`--accent`）
- 浅灰背景 `--code-bg`
- 灰色文字 `--muted`

### 13.4 表格

- 1px 灰色边框
- 表头浅灰背景 + 加粗
- 偶数行微灰背景
- 悬停行高亮 `--hover`

### 13.5 链接

- 蓝色（`--accent`），无下划线
- 悬停时出现底部细线

### 13.6 图片

- 最大宽度 100%，居中显示
- 微弱阴影 `0 2px 8px rgba(0,0,0,0.08)`
- 4px 圆角

---

## 14. 空状态设计

- 编辑区空状态：大号 emoji 图标（48px，透明度 0.3）+ 提示文字 + 副标题
- 侧边栏空状态：居中灰色文字「没有打开的文件夹」
- 列表空状态：居中灰色文字「暂无历史记录」等

---

## 15. 总结：风格关键词

| 关键词 | 体现 |
|--------|------|
| **极简** | 无边框输入框、细滚动条、最少的视觉元素 |
| **克制** | 低饱和度色板、微弱阴影、短过渡时间 |
| **沉浸** | 纯白背景、内容优先、控件"隐形" |
| **精致** | 统一的圆角体系、一致的间距节奏、细腻的悬停反馈 |
| **Typora 风** | 衬线体 Markdown 正文、代码块深色主题、引用块蓝色竖线 |
| **Windows 原生感** | 自定义标题栏、Segoe UI 字体、系统化窗口控制按钮 |
