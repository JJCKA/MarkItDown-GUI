# 更新日志

## v2.1.0 — 2026-05-09

### 新功能

- **拖拽支持**：直接拖拽文件或文件夹到窗口即可打开，支持全窗口拖放浮层提示
- **转换队列管理**：批量转换时显示队列面板，支持逐文件状态追踪、取消剩余任务、失败重试、全部导出
- **源文件 vs 转换结果对比**：新增对比视图，左侧显示 markitdown 原始提取文本，右侧显示最终转换结果（LLM 增强后），可拖拽调整分屏比例
- **Markdown 源码编辑**：源码模式下可直接编辑 Markdown 文本，预览模式实时渲染更新
- **转换统计面板**：记录总转换次数、总字符数、LLM 调用次数、成功率、平均耗时，数据持久化到 localStorage
- **历史记录缓存**：转换结果自动缓存到 `~/.markitdown-ui/cache.json`，支持配置最大缓存条数，历史记录点击直接查看转换结果（跨会话有效）
- **页面标题栏**：设置、历史记录、统计页面新增顶部标题栏 + 关闭按钮

### 改进

- **Typora 风格 Markdown 渲染**：全新 CSS 渲染样式，包括深色代码块、表格斑马纹、任务列表复选框、图片居中阴影、引用块样式等
- **代码块语言标签**：代码块右上角显示语言名称
- **任务列表支持**：`- [x]` 和 `- [ ]` 渲染为勾选框样式
- **侧边栏分割线优化**：可点击区域扩大到 5px，鼠标悬停时线条变蓝变粗
- **进度条加宽**：转换进度条从 120px 扩大到 1200px，更易观察
- **时间戳显示**：历史记录显示转换时间（年/月/日 时:分）
- **缓存状态区分**：历史记录中已缓存项带淡蓝色背景标记

### 安全修复

- `/api/export` 端点增加路径校验，仅允许导出 `.md` 文件
- `test_llm_connection` 不再修改全局配置对象

### Bug 修复

- 修复 `asyncio.get_event_loop()` 废弃警告，改为 `asyncio.get_running_loop()`
- 修复设置页自动保存在每次按键时触发请求的问题（增加 500ms 防抖）
- 修复历史记录不显示的 bug（`get_history` 端点未从磁盘重新加载数据）
- 修复点击历史记录后编辑器显示空白的 bug（App.tsx useEffect 状态竞争）
- 修复设置/历史/统计页面无法滚动的问题
- 修复侧边栏分割线与侧边栏之间的白色间隙
- 修复对比视图 header 下多余边框线
- 修复菜单栏预览/源码选项行为不正确的问题

---

## v2.0.0 — 2026-05-01

### 重大更新

- **架构重构**：从 Python tkinter 迁移到 Electron + React + FastAPI 架构
- **全新界面**：Typora 风格极简界面，自定义标题栏、菜单栏、侧边栏
- **LLM 增强**：支持图片描述、音频转录、全文总结、复杂表单清洗
- **批量转换**：多选文件批量转换，SSE 实时进度推送
- **SSE 实时日志**：转换过程中的日志实时推送到前端

### 技术栈

| 层 | 技术 |
|---|------|
| 桌面壳 | Electron |
| 前端 | React 18 + TypeScript + Zustand + marked |
| 后端 | Python 3.13 + FastAPI + markitdown + httpx |
| 打包 | PyInstaller + electron-builder |

---

## v1.0.0 — 初始版本

- 基于 Python tkinter 的桌面应用
- 支持基本文件转换为 Markdown
- 支持 PDF、Word、Excel、PPT、图片等格式
